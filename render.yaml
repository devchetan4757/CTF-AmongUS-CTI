services:
  - type: web
    name: the-imposter-api1
    runtime: python
    plan: free
    buildCommand: pip install --no-cache-dir -r backend/requirements.txt
    startCommand: cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: FRONTEND_ORIGIN
        value: https://capture-the-imposter1.onrender.com

  - type: web
    name: capture-the-imposter1
    runtime: static
    buildCommand: cd frontend && npm ci && npm run build
    staticPublishPath: frontend/dist
    envVars:
      - key: VITE_API_URL
        value: ""
    routes:
      - type: rewrite
        source: /api/*
        destination: https://the-imposter-api1.onrender.com/api/*
      - type: rewrite
        source: /site/*
        destination: https://the-imposter-api1.onrender.com/site/*
      - type: rewrite
        source: /security/*
        destination: https://the-imposter-api1.onrender.com/security/*
      - type: rewrite
        source: /*
        destination: /index.html
